# Docker Compose configuration for the Data Reliability Platform.
# This file will be expanded in future sessions to run:
# - online PostgreSQL database
# - offline PostgreSQL database
# - Airflow
# - supporting services

services:
  placeholder:
    image: alpine:latest
    command: ["echo", "Data Reliability Platform Docker placeholder"]
